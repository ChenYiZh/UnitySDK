#pragma once

#include "CoreMinimal.h"

#include "ThisGame/Framework/Decorators/Table/TableScheme.h"
#include "ThisGame/Framework/Interfaces/TableData.h"

#include "UObject/Object.h"
#include "Table{0}.generated.h"

UCLASS(BlueprintType)
class THISGAME_API UData{0} : public UObject, public ITableData
{
	GENERATED_BODY()

private:
{1}

public:
{2}

public:
	virtual void Parse(TArray<FString> Line) override;
};


/**
 * 
 */
UCLASS(BlueprintType)
class THISGAME_API UTable{0} : public UTableScheme
{
	GENERATED_BODY()

private:
	TMap<int, UData{0}*> Table;

public:
	virtual void OnCreate_Implementation(UParameter* Param) override;
	virtual void Parse(TArray<TArray<FString>> FileLines) override;
	virtual void OnDestroy_Implementation() override;
	UFUNCTION(BlueprintCallable, Category="{0}")
	static UData{0}* Get(int Key);
	UFUNCTION(BlueprintCallable, Category="{0}")
	static TMap<int, UData{0}*> Map();
};